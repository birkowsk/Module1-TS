import { filterWith, data } from './task';

function main() {
  console.log(filterWith(data, 'Mercado'));
  // console.log(filterWith(data,'tag'));
  // console.log(filterWith(data,'id'));
}

export default main;
